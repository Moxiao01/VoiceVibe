# Voice Vibe v0.2.1 更新公告

## 修复

- 修复在「按住说话」模式下单击 F2、说话时间过短时，程序可能死机的问题。
- 短按热键后的识别会话改为异步清理，避免识别 SDK 的同步错误回调与状态锁互相等待。

## 安装

下载附件 `VoiceVibe-Setup-0.2.1.exe` 双击安装（含全部运行依赖，无需 Python）。

已安装旧版本的用户可直接覆盖安装，配置和历史记录保存在 `%APPDATA%\VoiceVibe`，不会受影响。
